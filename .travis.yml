language: python
python:
  - "3.9"

services:
  - docker
  - postgresql
  - redis

branches:
  only:
    - travis

before_script:
  - docker compose -f docker-test.yaml up -d

script:
  - python3 -m pytest tests/test.py

after_script:
  - docker compose down

notifications:
  email:
    recipients:
      - iryna.yarka@gmail.com
    on_success: never
    on_failure: always
